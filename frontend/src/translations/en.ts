// English translations (default language - mostly empty as English is the source)
// This file exists for consistency and potential future use

export const itemTranslations: Record<string, string> = {
  // English is the default, so no translations needed
};

export const materialTranslations: Record<string, string> = {
  // English is the default, so no translations needed
};

export const categoryTranslations: Record<string, string> = {
  // English is the default, so no translations needed
};

export const rarityTranslations: Record<string, string> = {
  // English is the default, so no translations needed
};

